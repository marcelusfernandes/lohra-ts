# Arquivos ignorados pelo git que cada worktree precisa (ADR 0004; skill worktree-segura).
# Hoje: nenhum. Segredos vivem em ~/.lohra/.env (LOHRA_HOME), fora do repo; dist/ é
# reconstruído por `npm run build`; node_modules vem por worktree.symlinkDirectories.
