{
  "input": "Say hello.",
  "stub_script": { "default": [{ "kind": "text", "content": "Hello!" }] },
  "mechanism": [
    { "kind": "request_count", "count": 1 },
    {
      "kind": "system_prompt_includes",
      "substring": "You are Lohra, a self-improving AI assistant."
    },
    { "kind": "envelope_pointer", "pointer": "/completed", "value": true }
  ],
  "outcome": {
    "question": "Does the model reply with a greeting?",
    "expect": "Hello"
  },
  "budget_tokens": 11000,
  "note": "Sentinela do harness em si — a identidade DEFAULT_IDENTITY hoje (src/context/system-prompt.ts:1-5). #579 (mergeado) NÃO mudou esse texto — adicionou DOCTRINE_CORE/EXTENDED depois da identidade, na mesma faixa `stable` (src/context/doctrine.ts) — então esta assertion continua correta como está; ver final-answer-avoids-promise.json e child-task-fails-silently.json para os casos que exercitam a doutrina nova."
}
