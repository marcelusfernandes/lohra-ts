/** Compatibility re-export for the server callers that originally owned this
 * helper. Headless modules import the serialization-layer implementation. */
export { isPythonFalsy, isPythonTruthy } from "../serialization/python-truthy.js";
