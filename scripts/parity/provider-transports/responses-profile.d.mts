import type { ProviderProfile } from "../../../src/providers/types.js";

/** Checked TypeScript boundary for the side-effect-free runtime `.mjs`
 * module used by the responses live-smoke script and its parity test. */
export declare function resolveResponsesProfile(): ProviderProfile;
