// Stub (issue #151, test(red)): os 7 mutantes de `media/source.ts`,
// `media/errors.ts` e `tools/child.ts` ainda não migraram do runner
// antigo — ver `media-catalog-persistence.ts`.
import type { MediaMutant } from "./media-mutant.js";

export const otherMediaMutants: readonly MediaMutant[] = [];
